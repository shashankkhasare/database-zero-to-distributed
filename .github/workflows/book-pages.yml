name: Build and deploy the book

on:
  push:
    branches: [master]
    paths:
      - "book/**"
      - "book.toml"
      - ".github/workflows/book-pages.yml"
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Install mdBook 0.5.4
        run: |
          curl --proto '=https' --tlsv1.2 -sSfL \
            https://github.com/rust-lang/mdBook/releases/download/v0.5.4/mdbook-v0.5.4-x86_64-unknown-linux-gnu.tar.gz \
            | tar -xz -C "$RUNNER_TEMP"
          echo "$RUNNER_TEMP" >> "$GITHUB_PATH"
      - uses: actions/configure-pages@v5
      - name: Build book
        run: mdbook build --dest-dir build/book
      - name: Verify generated book
        run: node scripts/verify-book.mjs
      - uses: actions/upload-pages-artifact@v3
        with:
          path: build/book

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
